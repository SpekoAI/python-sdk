{
  "name": "sdk-python",
  "projectType": "library",
  "targets": {
    "lint": {
      "executor": "nx:run-commands",
      "options": { "command": "ruff check .", "cwd": "packages/sdk-python" }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": { "command": "command -v pytest >/dev/null && pytest || echo 'pytest not installed, skipping'", "cwd": "packages/sdk-python" }
    }
  }
}
